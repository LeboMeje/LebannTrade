import { CommerceAPIOperations } from '..';
interface ContentEditorActions {
    updatePreviewUrl: (url: string) => void;
    safeReaction<T>(watchFunction: () => T, reactionFunction: (arg: T) => void, options?: {
        fireImmediately: true;
    }): void;
}
export declare const onEditorLoad: (config: any, apiOperations: CommerceAPIOperations, resourceName: string) => ({ safeReaction, updatePreviewUrl }: ContentEditorActions) => void;
export {};
